<%= render 'refinery/admin/search_header', :url => refinery.user_histories_admin_user_histories_path %>
<div class='pagination_container'>
  <% if @user_histories.any? %>
    <%= render 'user_histories' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
