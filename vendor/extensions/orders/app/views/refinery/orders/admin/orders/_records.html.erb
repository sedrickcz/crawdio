<%= render 'refinery/admin/search_header', :url => refinery.orders_admin_orders_path %>
<div class='pagination_container'>
  <% if @orders.any? %>
    <%= render 'orders' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
