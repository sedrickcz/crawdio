<%= render 'refinery/admin/search_header', :url => refinery.tiers_admin_tiers_path %>
<div class='pagination_container'>
  <% if @tiers.any? %>
    <%= render 'tiers' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
