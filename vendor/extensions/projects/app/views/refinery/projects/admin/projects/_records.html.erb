<%= render 'refinery/admin/search_header', :url => refinery.projects_admin_projects_path %>
<div class='pagination_container'>
  <% if @projects.any? %>
    <%= render 'projects' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
